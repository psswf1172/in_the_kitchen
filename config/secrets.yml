# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

shared:
  FB_APP_ID: 1607707252679927
  FB_APP_SECRET: ec82ea541bec8fd45306f92beaa4cd50
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 6e43b59a3412bb4daa15e8d19270ccb37c3274b0d4b0b5eb4691d452fe089422a553a37e593d429802973c3c2d240fcf188a00a4e1b0531fc1e21d21ca4e93a8

test:
  secret_key_base: 3476db93635fb7b205da784c91c1c68f9abc797e481a32a1dcf429c3726b43466efb6e9d2cf263b4dc23111cbf3dc8edb2a732508a15b4e4d7be679f25403f95

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
